// Modules
import { Request, Response, Router } from 'express';

// Providers
import { apiProvider } from '../provider/api.provider';

// Constants
const router: Router = Router();

/**
 * Add routing
 */
router.post('/positions', async (request: Request, response: Response) => {

});

export default Router;